package template.megam

import org.apache.spark._
import org.apache.spark.SparkContext, SparkContext._
import com.typesafe.config.{Config, ConfigFactory}


/*
object HotelAnalysis  {
   def main(args: Array[String]) {
    //create spark context
    val sc = new SparkContext("local[4]", "Hotel-Analytics")
    val config = ConfigFactory.parseString("")
    //run the main hotel analysis
    val results = HotelAnalysisResult
    results.runJob(sc, config)
  }
} */
